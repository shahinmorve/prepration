// Define a function calculateArea that calculates and returns the area of a rectangle. It should take two parameters, length and width.

function calculatearea(length,width){
    return length*width;
}
let area=calculatearea(2,7);
console.log("area of reactangle",area);